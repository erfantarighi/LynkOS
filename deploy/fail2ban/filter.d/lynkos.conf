# Matches the `auth_failure ip=<ip> user=<user>` line emitted by
# app/routers/auth.py on failed /api/auth/login attempts.
[Definition]
failregex = auth_failure ip=<HOST> user=\S*\s*$
ignoreregex =
datepattern = {^LN-BEG}
