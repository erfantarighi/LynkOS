# Raspberry Pi OS / Debian 12+ log through systemd-journal, not /var/log/auth.log.
# Make every jail default to the systemd backend — individual jails can still
# override with their own `backend = ...` line.
[DEFAULT]
backend = systemd
